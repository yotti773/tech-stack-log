@AGENTS.md

## 開発ルール

- マイルストーン（M0〜M6）の作業がひとつ完了するたびに、`docs/技術スタック整理アプリ_構築手順書.md` の該当マイルストーンに実施内容（決定事項・URL・つまずいた点など）を追記し、完了条件をチェックする
- 決定の背景や経緯は `docs/技術スタック整理アプリ_構想と判断の記録.md` の「9. 実施ログ」に追記する
- この2点は、作業完了後にユーザーへ確認せず毎回自動で行う
- 認証まわりのルートハンドラや、バージョンによって書き方が変わりやすいAPI（`@supabase/ssr`、Next.jsのルーティング関連など）を実装する際は、記憶にあるパターンだけで書かず、事前に公式ドキュメントの最新版や `node_modules` 内のソース・型定義を確認し、それに沿った実装を行う。似て非なる別のユースケース向けサンプル（例: OAuthコールバック用とマジックリンク確認用）を混同しないよう、どのユースケースの説明かを確認してから実装する

## 開発フロー

フェーズ1（M0〜M6）は完了済み。以降の作業（フェーズ2の構想実装や個別の改善）は、`docs/技術スタック整理アプリ_構築手順書.md` のマイルストーン単位の追記に代わり、GitHub Issueで管理する — リポジトリのIssuesタブが「何が残っているか」の情報源になる。

- **非自明な作業は着手前にIssueを立てる。** 自分一人で作業する場合でも同様。`docs/技術スタック整理アプリ_構想と判断の記録.md` での意思決定と、それを実装したコードの対応関係を後から辿れるようにするため。
- **新しい作業ブランチを作成する際は、まず `git status` で作業ツリーがクリーンであることを確認し、`git checkout main && git pull --ff-only origin main` でローカルmainを最新化してから、そこから作業ブランチを切る。** mainに未コミットの変更がある、または `--ff-only` が失敗する（ローカルmainが分岐している）場合は、最新化を強行せずユーザーに確認する。
- ブランチ上で実装し、`npm run build`（型チェックを含む）と `npm run lint` で検証する。UI変更であれば `npm run dev` を起動して実ブラウザで動作確認する。
- **仕様に影響する変更を実装したら、対応する `docs/` の仕様書へ書き戻す。** マイルストーン完了時の書き戻し（上記「開発ルール」参照）に加えて、フェーズ2以降の非マイルストーン作業でも、仕様・データモデル・意思決定に関わる変更は `docs/技術スタック整理アプリ_構想と判断の記録.md` の該当章および「9. 実施ログ」に反映する。純粋なバグ修正・リファクタなど仕様に影響しない変更は書き戻し不要。
- **PR作成前にレビューを行う。** まとまった変更では `/code-review` skillを併用してよい。指摘は修正するか、対応しない場合は理由を残す。
- **Issueが完了したらPRを作成する。** PR本文で該当Issueを参照し（例: `Closes #6`）、マージ時に自動クローズされるようにする。PRを介さず `main` へ直接マージしない。
- **PRのマージはユーザーの明示的な指示（例:「マージして」）を受けてから行う。** マージ方式は常にsquash（PR内の複数コミットをmainに1つにまとめる）。
- **PRがマージされたら作業ブランチを削除する。** マージ確認後に `git checkout main && git pull --ff-only origin main && git branch -d <ブランチ名>` でローカルブランチを削除する。
- **Issue作成時は `優先度: 高` / `優先度: 中` / `優先度: 低` のいずれかを付ける。** 判断基準は `docs/技術スタック整理アプリ_構想と判断の記録.md` の「2. 目的とスコープ」（目的の優先順: 1. Next.js/Supabaseの実務理解、2. 自分の技術スタックの棚卸し、3. 将来のコミュニティ化）に沿う — フェーズ1相当の欠落・不具合や学習目的に直結するものほど高く、フェーズ2（コミュニティ機能）や「やらないこと」に近い拡張ほど低くする。
- **Issue作成時は優先度に加えてカテゴリラベル（`bug` / `ux` / `master` / `tech`）も1つ付ける。** `bug` = 予期しない動作・画面崩れ・データ不整合、`ux` = 情報の見せ方や操作手順の改善要望、`master` = `technologies` 共有マスタ（技術一覧）の登録・カテゴリ分類の見直し、`tech` = ドキュメント更新・依存更新・デバッグ利便性などの技術改善。GitHubのWeb画面から作る場合は `.github/ISSUE_TEMPLATE/` のテンプレートがカテゴリラベルを自動付与するが、優先度ラベルはテンプレートでは付与されないため作成時に手動で追加する。
